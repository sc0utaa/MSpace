<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Album Rater</title>
    <style>
        :root {
            --primary-color: #5D3FD3;
            --secondary-color: #7D5BE5;
            --accent-color: #FFC107;
            --text-color: #333;
            --light-bg: #f9f9f9;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: var(--text-color);
            margin: 0;
            padding: 0;
        }
        
        .hero {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
            padding: 5rem 0;
            text-align: center;
        }
        
        .hero-title {
            font-size: 3.5rem;
            margin-bottom: 1rem;
            font-weight: 700;
        }
        
        .hero-subtitle {
            font-size: 1.5rem;
            margin-bottom: 2rem;
            font-weight: 300;
            max-width: 700px;
            margin-left: auto;
            margin-right: auto;
        }
        
        .cta-button {
            background-color: var(--accent-color);
            color: var(--text-color);
            font-weight: 600;
            padding: 0.75rem 2rem;
            border-radius: 50px;
            text-decoration: none;
            display: inline-block;
            transition: all 0.3s ease;
            border: none;
            font-size: 1.1rem;
            cursor: pointer;
        }
        
        .cta-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 4px 8px rgba(0,0,0,0.2);
        }
        
        .features {
            padding: 5rem 0;
            background-color: var(--light-bg);
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 1rem;
        }
        
        .section-title {
            text-align: center;
            margin-bottom: 3rem;
            font-size: 2.2rem;
            color: var(--primary-color);
        }
        
        .feature-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
        }
        
        .feature-card {
            background: white;
            border-radius: 10px;
            padding: 2rem;
            text-align: center;
            box-shadow: 0 5px 15px rgba(0,0,0,0.05);
            transition: transform 0.3s ease;
        }
        
        .feature-card:hover {
            transform: translateY(-5px);
        }
        
        .feature-icon {
            font-size: 2.5rem;
            color: var(--primary-color);
            margin-bottom: 1rem;
        }
        
        .feature-title {
            font-size: 1.5rem;
            margin-bottom: 1rem;
        }
        
        .feature-text {
            color: #666;
        }
        
        .popular-section {
            padding: 5rem 0;
        }
        
        .album-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            gap: 1.5rem;
        }
        
        .album-card {
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
        }
        
        .album-card:hover {
            transform: translateY(-5px);
        }
        
        .album-image {
            width: 100%;
            height: 200px;
            object-fit: cover;
            display: block;
        }
        
        .album-info {
            padding: 1rem;
        }
        
        .album-title {
            font-size: 1.1rem;
            margin: 0 0 0.5rem;
        }
        
        .album-artist {
            color: #666;
            margin: 0 0 0.5rem;
        }
        
        .rating {
            display: flex;
            align-items: center;
            color: var(--accent-color);
        }
        
        .footer {
            background-color: #222;
            color: #fff;
            padding: 2rem 0;
            text-align: center;
        }
        
        
        
    </style>
</head>
<body>
    <section class="hero">
        <div class="container">
            <h1 class="hero-title">Album Rater</h1>
            <p class="hero-subtitle">Discover, rate, and track your favorite music albums. Join our community of music enthusiasts and find your next favorite album.</p>
            <a href="/Albums" class="cta-button">Start Rating</a>
        </div>
    </section>

    <section class="features">
        <div class="container">
            <h2 class="section-title">Why Use Album Rater?</h2>
            <div class="feature-grid">
                <div class="feature-card">
                    <div class="feature-icon">🔍</div>
                    <h3 class="feature-title">Discover New Music</h3>
                    <p class="feature-text">Find albums based on genre, artist, or community ratings to expand your musical horizons.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">⭐</div>
                    <h3 class="feature-title">Rate & Review</h3>
                    <p class="feature-text">Share your opinions and rate albums on a scale. Help others find great music.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">📊</div>
                    <h3 class="feature-title">Track Statistics</h3>
                    <p class="feature-text">See average ratings, most popular albums, and trending music in our community.</p>
                </div>
            </div>
        </div>
    </section>

   

    <footer class="footer">
        <div class="container">
            <p>&copy; @DateTime.Now.Year - Album Rater - All rights reserved</p>
        </div>
    </footer>
</body>
</html>
